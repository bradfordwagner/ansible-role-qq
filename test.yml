---
- hosts: localhost
  roles:
    - role: '{{ playbook_dir }}'
  tasks:
    - shell: echo '{"hello":"world"}' | qq .hello
      register: test_output
    - name: assert binary prints help output
      debug:
        msg: '{{ test_output.stdout }}'
