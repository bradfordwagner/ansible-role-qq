# bradfordwagner.ansible-role-qq

## Updating the version

Version is controlled by `qq_version` in `defaults/main.yml`.

Check for the latest release at: https://github.com/JFryy/qq/releases

```yaml
qq_version: vX.Y.Z
```
